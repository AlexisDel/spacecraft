algo :

T = tableau des montagnes (tableau de couleurs), initialisé à nonvue ou montagne
flag = true
while(flag):
	flag = false
	T = color(T, pos) // fonction colorant le tableau avec toutes les cases accessibles à partir de pos
	parcours de T:
		si t == nonvue //case pas colorée par color()
		flag == true //on iterera une fois de plus
		pos' = find color(pos) // fonction trouvant la case colorée la plus proche de pos
		creuse(pos, pos', T) // fonction modifiant T pour créer un chemin vide de pos vers pos'
		break de la boucle